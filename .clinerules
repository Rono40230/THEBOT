# THEBOT - Règles Essentielles de Développement

## ⚠️ IMPÉRATIF - Ne JAMAIS enfreindre

### 🎯 RÈGLES DE COMPORTEMENT CRITIQUE

**PROFIL UTILISATEUR : Non-codeur - Claude modifie directement les fichiers dans VSCode**

1. **Structure modulaire OBLIGATOIRE**
   - ✅ TOUTE implémentation DOIT respecter l'architecture modulaire existante
   - ✅ Suivre EXACTEMENT le pattern `[category]/[name]/{__init__.py, config.py, calculator.py, plotter.py}`
   - ❌ JAMAIS créer de nouvelle structure sans validation explicite

2. **Ne JAMAIS casser le code existant**
   - ✅ Tester l'impact sur le code existant AVANT toute modification
   - ✅ Si une modification risque de casser quelque chose : DEMANDER confirmation avant d'agir
   - ❌ AUCUNE modification du code fonctionnel sans approbation explicite
   - ✅ **AVERTIR** clairement si une modification touche du code sensible

3. **Zéro initiative non sollicitée**
   - ❌ INTERDICTION ABSOLUE de proposer des "améliorations" non demandées
   - ❌ Ne JAMAIS refactoriser sans demande explicite
   - ❌ Ne JAMAIS "optimiser" le code sans instruction
   - ✅ Faire UNIQUEMENT ce qui est demandé, rien de plus
   - ✅ **EXCEPTION** : Signaler brièvement les opportunités d'optimisation CRITIQUES (>20% performance) SANS les implémenter

4. **Communication adaptée aux non-codeurs**
   - ✅ **MODIFICATIONS DIRECTES** : Claude modifie les fichiers directement dans VSCode
   - ✅ **FEEDBACK MINIMAL** : Après modification réussie, message court type "✅ Modifié : [fichier]"
   - ✅ **EXPLICATIONS CIBLÉES** : Expliquer uniquement les décisions importantes ou concepts nouveaux
   - ✅ **OPTIMISATIONS** : Suggérer optimisations si impact significatif (>20%) SANS les appliquer automatiquement
   - ❌ **PAS de résumé** détaillé de ce qui a été changé (l'utilisateur voit les changements dans VSCode)
   - ❌ **PAS de jargon** technique non expliqué
   - ❌ **PAS de documentation** pas de création de document explicatif sauf sur demande

5. **Gestion des bugs récurrents**
   - ✅ Après **5 tentatives échouées** sur la même correction : CHANGER DE STRATÉGIE
   - ✅ Reconnaître explicitement : "❌ Ma logique est incorrecte, je teste une autre approche"
   - ✅ **EXPLIQUER** brièvement pourquoi la nouvelle approche est différente (2-3 lignes max)
   - ✅ Proposer UNE alternative radicalement différente
   - ❌ Ne JAMAIS répéter la même logique en espérant un résultat différent

6. **Économie de tokens intelligente**
   - ✅ Priorité tokens pour : MODIFICATIONS CODE + EXPLICATIONS CRITIQUES + OPTIMISATIONS
   - ❌ Éviter : Longs résumés de modifications, explications évidentes, théorie excessive
   - ✅ Format ultra-concis : l'utilisateur VOIT les changements dans VSCode
   - ✅ Utiliser des emojis (✅❌⚠️⚡) pour clarté visuelle rapide

### Format de réponse OBLIGATOIRE

**Pour une modification simple (95% des cas) :**
```
✅ Modifié : `src/thebot/indicators/basic/sma/calculator.py`
```

**Pour une modification avec point important :**
```
✅ Modifié : `src/thebot/indicators/basic/rsi/calculator.py`

💡 Utilise maintenant Decimal pour éviter erreurs d'arrondi sur prix.
```

**Pour correction après erreur :**
```
✅ Corrigé : `chemin/vers/fichier.py`
```

**Pour bug récurrent (5+ échecs) :**
```
❌ Ma logique pandas.ewm() ne fonctionne pas.
Nouvelle approche : calcul manuel avec numpy (plus stable).

✅ Modifié : `src/thebot/indicators/basic/ema/calculator.py`
```

**Pour suggérer optimisation CRITIQUE :**
```
⚡ OPTIMISATION : Calcul actuel 2.5s → possible 0.3s avec vectorisation numpy (gain 88%).

Veux-tu que je l'implémente ?
```

**Pour demander confirmation avant modification sensible :**
```
⚠️ Cette modification va toucher `core/types.py` utilisé par 15 modules.
Risque de casser le code existant.

Veux-tu que je continue ?
```

---

## 🏗️ Architecture ultra-modulaire (RÈGLE D'OR)

### Pattern pour TOUS les indicateurs
Chaque indicateur DOIT suivre cette structure exacte :

```
src/thebot/indicators/[category]/[name]/
├── __init__.py          # Orchestration et API publique
├── config.py            # Configuration Pydantic (validation)
├── calculator.py        # Logique de calcul pure (Single Responsibility)
└── plotter.py           # Visualisation (optionnel)
```

### Principes architecturaux
- **Single Responsibility** : Un module = une responsabilité unique
- **Separation of Concerns** : Séparer config / calcul / visualisation
- **Interface Contracts** : Tous les indicateurs héritent de `base.indicator.Indicator`
- **Dependency Injection** : Pas de dépendances hardcodées
- **Factory Pattern** : Utiliser `manager.factory` pour créer les indicateurs

### Categories d'indicateurs
- `basic/` : SMA, EMA, ATR, RSI, OBV, SuperTrend
- `momentum/` : Squeeze, Patterns, Breakout
- `structural/` : FVG, Support/Resistance, Fibonacci, Order Blocks
- `smart_money/` : Market Structure, Liquidity, Sessions
- `volume/` : Volume Profile
- `analysis/` : TrendScore, Regime, MTF

---

## 🎯 Spécificités THEBOT

### Focus stratégies
1. **Scalping** (priorité) : Timeframes 1m, 5m, 15m
2. **Day Trading** : Timeframes 1h, 4h
3. **Swing Trading** : Timeframes 1D, 1W
4. **Position Trading** : Timeframes 1M

### Marchés supportés
- **Crypto** : BTC, ETH, altcoins majeurs (Binance)
- **Forex** : Paires majeures EUR/USD, GBP/USD, USD/JPY, etc. (Alpha Vantage)

### Événements économiques
- Intégrer systématiquement le calendrier économique
- Analyser impact annonces vs mouvements prix
- Alertes pré/post événements importants
- Corrélation événements ↔ volatilité

### Translation indicateurs NonoBot (Rust → Python)
- **Source** : https://github.com/Rono40230/NonoBot
- **Objectif** : 25 indicateurs à traduire
- **Méthode** : Analyse algorithme Rust → Implementation Python pure
- **Validation** : Tester parité résultats Rust vs Python
- **Documentation** : Référencer l'indicateur Rust source

---

## ✅ Checklist avant chaque commit

- [ ] Code formaté avec Black
- [ ] Imports triés avec isort
- [ ] Type hints présents
- [ ] Docstrings Google style
- [ ] Tests unitaires passent
- [ ] Coverage maintenu/amélioré
- [ ] Pas de print() en production
- [ ] Pas de credentials en dur
- [ ] Logs appropriés ajoutés
- [ ] Documentation mise à jour si nécessaire
- [ ] **VÉRIFIÉ : Aucun code existant cassé**
- [ ] **VÉRIFIÉ : Structure modulaire respectée**

---

## 🚫 Interdictions strictes

### À ne JAMAIS faire
- ❌ Credentials/API keys en dur dans le code
- ❌ `print()` à la place de `logger`
- ❌ Float pour calculs de prix (utiliser Decimal)
- ❌ Ignorer les exceptions (`except: pass`)
- ❌ Code non testé en production
- ❌ Boucles Python sur DataFrames (vectoriser)
- ❌ Importer tout un module (`from module import *`)
- ❌ Variables globales mutables
- ❌ Modification des DataFrames en place sans `.copy()`
- ❌ Commits sans message clair

---

## 💡 Bonnes pratiques encouragées

### À privilégier
- ✅ Type hints partout
- ✅ Validation Pydantic pour configs
- ✅ Async/await pour I/O
- ✅ Pattern Repository pour DB
- ✅ Factory Pattern pour indicateurs
- ✅ Caching intelligent
- ✅ Logging exhaustif
- ✅ Tests automatisés
- ✅ Documentation claire
- ✅ Code review systématique

---

## 🎯 Rappels importants pour Claude

1. **Modularité** : Respecter STRICTEMENT l'architecture modulaire des indicateurs
2. **Précision** : Utiliser Decimal pour prix, valider avec 15+ tests par indicateur
3. **Performance** : Vectoriser avec numpy/pandas, pas de loops Python
4. **Robustesse** : Gérer TOUTES les erreurs, timeout sur APIs, retry logic
5. **Documentation** : Docstrings Google style, type hints obligatoires
6. **Tests** : Coverage > 80%, tests unitaires pour chaque indicateur
7. **Sécurité** : Jamais de credentials en dur, validation inputs
8. **Async** : Utiliser async/await pour toutes opérations I/O
9. **Logging** : Logger.info/debug/error, jamais print()
10. **Git** : Commits conventionnels, branches feature/fix

---

**Version** : 2.0 - Simplifié
**Dernière mise à jour** : Simplification pour focus essentiel
**Auteur** : Documentation THEBOT trading bot
